<? php

  $user_id_array=array("zhang","wang");
  $user_id_date = array('' => array() );

  echo "string";


   // foreach ($result as $each_line) {

   //          foreach ($user_id_array as $key) {

   //              if($each_line->user_id == $key)
   //              {
   //                  if(array_key_exists($key, $user_id_date))
   //                  {
   //                      $get[] = $user_id_date[$key];
   //                      $temparray = array_push($each_line->in_date,$get);
   //                      array_push(array, var);

   //                  }else{

   //                      $temparray = array($each_line->in_date);

   //                      array_push($user_id_date, $key->temparray);
   //                  }
   //              }
   //          }

?>
